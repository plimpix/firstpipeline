pipeline {
    agent { /usr/local/bin/docker { image 'ruby' } } 
    stages {
        stage('build') {
            steps {
                sh 'echo "im just curious on what this does"'
            }
        }
    }
}
