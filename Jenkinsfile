pipeline {
    agent { docker { image 'ruby' } } 
    stages {
        stage('build') {
            steps {
                sh 'echo "im just curious on what this does"'
            }
        }
    }
}
